
------------------------------------------------------------------------------------------------------------------
This app has this minor bug in which inquirer is being executed prior to displayProducts() function resulting in a
problem at the beginning.Although this bug could not be fixed it does not 'disturb' the logic and the functionality
behind the app.
------------------------------------------------------------------------------------------------------------------
After you load the database from products.sql you fill in the password and execute the command 
node bamazonCustomer.js